#include<stdio.h>

main(){
    int valores[5];

    for(; int i =0; i < 5; i++){
        printf("\nDigite os valores: ");
        scanf("%d", &valores[i]);

    }
     //mostra os numeros do maior para o menor
    for(int i = 4; i => 0; i--){
        printf("\n%d",valores[i]);

    }
}